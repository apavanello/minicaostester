apiVersion: v1
kind: Service
metadata:
  name: {{ include "chaos-target.fullname" . }}
  labels:
    {{- include "chaos-target.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chaos-target.selectorLabels" . | nindent 4 }}
